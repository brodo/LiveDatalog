# Domain docs

This is a single-context repository. Before exploring code, engineering skills should read these resources when they exist:

- `CONTEXT.md` at the repository root
- ADRs under `docs/adr/` that touch the area being changed

Missing domain files are not an error; proceed silently. Use vocabulary defined in `CONTEXT.md`, and surface any proposed change that conflicts with an existing ADR.
